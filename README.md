# flood-prediction
# Flood-prediction – Explainable Hyperlocal Flood Risk Simulator for Chennai

FloodXplain is the first hackathon-ready tool that not only predicts Chennai flood risk but explains exactly why (using SHAP) and lets users simulate "what-if" mitigation actions like clearing drains to see risk drop instantly.

Run with uvicorn backend.app:app --reload
